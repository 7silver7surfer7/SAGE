# --- secrets: never commit ---
.env
.env.*
!.env.sample
*.pem
*.key
**/arweave*.json
# generated agent wallets (private keys + ready-to-paste MCP configs)
sage-mcp/agents/wallets/

# --- dependencies / build output ---
node_modules/
.next/
out/
dist/
artifacts/
cache/
typechain-types/

# --- misc ---
.DS_Store
*.log
tsconfig.tsbuildinfo

# runtime state, logs, local data — never ship
backups/
atelier_out/
deploy/**/launchd.out
deploy/**/launchd.err
deploy/social-drip/state.json
Sage-UI-main/.claude/
*.crypto.bak
sage-agents/agents.log.jsonl
sage-agents/.env
# agent wallet PRIVATE KEYS — absolutely never
sage-mcp/agents/wallets/
